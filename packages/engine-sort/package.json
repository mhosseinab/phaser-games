{
  "name": "@blublux/engine-sort",
  "version": "0.0.0",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@blublux/engine": "workspace:^"
  }
}