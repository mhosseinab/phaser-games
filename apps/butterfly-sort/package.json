{
  "name": "@blublux/butterfly-sort",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:native": "vite build --mode native",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo \"Lint skipped\"",
    "sync": "pnpm build:native && cap sync",
    "open:ios": "cap open ios",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@blublux/engine": "workspace:*",
    "phaser": "^4.2.0",
    "@capacitor/core": "^8.4.0",
    "@capacitor-community/admob": "^8.0.0",
    "@blublux/ads-adapter": "workspace:*",
    "@blublux/engine-sort": "workspace:*"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.0",
    "@capacitor/ios": "^8.4.0",
    "@capacitor/android": "^8.4.0",
    "@capacitor/assets": "^3.0.0",
    "vite": "^6.0.0",
    "vitest": "^2.0.0",
    "typescript": "^5.5.0"
  }
}